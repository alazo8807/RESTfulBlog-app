<% include partials/header %>

<h1>This is the index page</h1>

<% blogs.forEach( function(value){ %>
    <div>
        <h1><%= value.title %></h1>
        <img src="<%= value.image %>"></img>
        <spanp> <%= value.created %></span>
        <p><%= value.body %></p>
    </div>    
<% }); %>

<% include partials/footer %>